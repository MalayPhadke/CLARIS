# Security Configuration - REQUIRED for production

# JWT Secret Key - Used for signing authentication tokens
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET_KEY=your-secret-jwt-key-here

# Encryption Master Key - Used for encrypting stored passwords
# Generate with: python -c "import secrets, base64; print(base64.urlsafe_b64encode(secrets.token_bytes(32)).decode())"
ENCRYPTION_MASTER_KEY=your-encryption-master-key-here

# IMPORTANT SECURITY NOTES:
# 1. Never commit .env file to version control
# 2. Keep these keys secure and rotate them periodically
# 3. Use different keys for development and production
# 4. Store production keys in secure secret management system (e.g., AWS Secrets Manager, HashiCorp Vault)
